name: ${project.name}
version: ${project.version}
description: '${project.description}'
depend: [KotlinRuntime, Vault]
softdepend: []
authors: [${project.authors}]
website: ${project.url}
main: ${project.mainClass}
